# Environment Variables Configuration

# This file shows example environment variables for the Beautify Me application.
# Copy this file to .env and fill in your actual values.
# IMPORTANT: Never commit the .env file to version control!

# ========================================
# API Keys (Currently Optional)
# ========================================

# Google Gemini API Key (for AI features - future enhancement)
# Get your key at: https://makersuite.google.com/app/apikey
# REACT_APP_GEMINI_API_KEY=your_api_key_here

# ========================================
# Application Settings
# ========================================

# Development server port (default: 3000)
# PORT=3000

# Enable/disable debug mode
# REACT_APP_DEBUG=false

# ========================================
# Camera Settings (Optional)
# ========================================

# Default camera resolution width
# REACT_APP_CAMERA_WIDTH=1280

# Default camera resolution height
# REACT_APP_CAMERA_HEIGHT=720

# ========================================
# Notes for Beginners
# ========================================

# 1. Environment variables in Create React App must start with REACT_APP_
# 2. After changing .env, restart the development server (npm start)
# 3. Variables are embedded at build time, not runtime
# 4. Use process.env.REACT_APP_VARIABLE_NAME to access in code
# 5. Never commit sensitive data like API keys to Git

# Example usage in code:
# const apiKey = process.env.REACT_APP_GEMINI_API_KEY;
# if (!apiKey) {
#   console.warn('API key not configured');
# }
